<div class="col-lg-8 col-md-10 col-sm-12 shadow p-3">
<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block", class: "form-lable my-2" %>
    <%= form.text_field :title, class: "form-control" %>
  </div>

  <div>
    <%= form.label :conntent, style: "display: block", class: "form-lable my-2" %>
    <%= form.text_area :conntent, rows: 15, class: "form-control" %>
  </div>

  <div>
    <%= form.number_field :user_id, id: :post_user_id, 
    value: current_user.id , type: :hidden %>
  </div>

  <div>
    <%= form.submit class: "btn btn-primary my-2" %>
  </div>
<% end %>

</div>
